en:
  activerecord:
    attributes:
      address:
        fu: FU
        ibge_code: IBGE Code
        public_place: Public Place
    errors:
      the_date_entered_is_greater_than_the_current_date: The date entered is greater than the current date.